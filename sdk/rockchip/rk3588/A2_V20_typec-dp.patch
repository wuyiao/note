diff --git a/arch/arm64/boot/dts/rockchip/rk3588-evb7-lp4-v10-linux.dts b/arch/arm64/boot/dts/rockchip/rk3588-evb7-lp4-v10-linux.dts
index cbc3e1687e50..748e8b6321e1 100644
--- a/arch/arm64/boot/dts/rockchip/rk3588-evb7-lp4-v10-linux.dts
+++ b/arch/arm64/boot/dts/rockchip/rk3588-evb7-lp4-v10-linux.dts
@@ -256,6 +256,82 @@ &i2c7 {
 	pinctrl-names = "default";
 	pinctrl-0 = <&i2c7m3_xfer>;
 
+	usbc1: fusb302@22 {
+		compatible = "fcs,fusb302";
+		reg = <0x22>;
+		//interrupt-parent = <&gpio0>;
+		//interrupts = <RK_PB0 IRQ_TYPE_LEVEL_LOW>;
+		interrupt-parent = <&gpio4>;
+		interrupts = <RK_PA3 IRQ_TYPE_LEVEL_LOW>;
+		pinctrl-names = "default";
+		pinctrl-0 = <&usbc1_int>;
+		vbus-supply = <&vbus5v0_typec1>;
+		//phy-supply = <&vbus5v0_typec1>;
+		// ctl1-5V-gpios = <&gpio1 RK_PA2 GPIO_ACTIVE_HIGH>;
+		// ctl1-20V-gpios = <&gpio1 RK_PA4 GPIO_ACTIVE_HIGH>;
+		status = "okay";
+
+		ports {
+			#address-cells = <1>;
+			#size-cells = <0>;
+
+			port@0 {
+				reg = <0>;
+				usbc1_role_sw: endpoint@0 {
+					remote-endpoint = <&dwc3_1_role_switch>;
+				};
+			};
+		};
+
+		usb_con1: connector {
+			compatible = "usb-c-connector";
+			label = "USB-C";
+			data-role = "dual";
+			power-role = "dual";
+			try-power-role = "sink";
+			op-sink-microwatt = <1000000>;
+			sink-pdos =
+				<PDO_FIXED(5000, 1000, PDO_FIXED_USB_COMM)>;
+				/*<PDO_FIXED(5000, 3000, PDO_FIXED_USB_COMM)
+				 PDO_FIXED(9000, 3000, PDO_FIXED_USB_COMM)
+				 PDO_FIXED(12000, 3000, PDO_FIXED_USB_COMM)>;*/
+
+			source-pdos =
+				<PDO_FIXED(5000, 3000, PDO_FIXED_USB_COMM)>;
+
+			altmodes {
+				#address-cells = <1>;
+				#size-cells = <0>;
+
+				altmode@0 {
+					reg = <0>;
+					svid = <0xff01>;
+					vdo = <0xffffffff>;
+				};
+			};
+
+			ports {
+				#address-cells = <1>;
+				#size-cells = <0>;
+
+				port@0 {
+					reg = <0>;
+					usbc1_orien_sw: endpoint {
+						remote-endpoint = <&usbdp_phy1_orientation_switch>;
+					};
+				};
+
+				port@1 {
+					reg = <1>;
+					dp1_altmode_mux: endpoint {
+						remote-endpoint = <&usbdp_phy1_dp_altmode_mux>;
+					};
+				};
+			};
+
+		};
+	};
+
 	lt6911uxe: lt6911uxe@2b {
 		status = "okay";
 		reg = <0x2b>;
diff --git a/arch/arm64/boot/dts/rockchip/rk3588-evb7-lp4.dtsi b/arch/arm64/boot/dts/rockchip/rk3588-evb7-lp4.dtsi
index 63b838e96719..57d078870098 100644
--- a/arch/arm64/boot/dts/rockchip/rk3588-evb7-lp4.dtsi
+++ b/arch/arm64/boot/dts/rockchip/rk3588-evb7-lp4.dtsi
@@ -214,6 +214,19 @@ vbus5v0_typec0: vbus5v0-typec0 {
                 pinctrl-names = "default";
                 pinctrl-0 = <&typec5v_pwren>;
         };
+	vbus5v0_typec1: vbus5v0-typec1 {
+		compatible = "regulator-fixed";
+		regulator-name = "vbus5v0_typec1";
+		regulator-min-microvolt = <5000000>;
+		regulator-max-microvolt = <5000000>;
+		enable-active-high;
+		//regulator-always-on; /* add */
+		gpio = <&gpio1 RK_PA7 GPIO_ACTIVE_HIGH>;
+		vin-supply = <&vcc5v0_usb>;
+		pinctrl-names = "default";
+		pinctrl-0 = <&typec5v_pwren1>;
+		//pinctrl-0 = <&typec5v_pwren1 &ctl1_5v &ctl1_20v>;
+	};
 
 	vcc_mipicsi0: vcc-mipicsi0-regulator {
 		compatible = "regulator-fixed";
@@ -737,12 +750,17 @@ vcc5v0_host_en: vcc5v0-host-en {
 	};
 
 	usb-typec {
+		usbc1_int: usbc1-int {
+			rockchip,pins = <4 RK_PA3 RK_FUNC_GPIO &pcfg_pull_up>;
+		};
+
+		typec5v_pwren1: typec5v-pwren1 {
+			rockchip,pins = <1 RK_PA7 RK_FUNC_GPIO &pcfg_pull_none>;
+		};
 		typec5v_pwren: typec5v-pwren {
-			//rockchip,pins = <3 RK_PB2 RK_FUNC_GPIO &pcfg_pull_none>;
 			rockchip,pins = <1 RK_PB1 RK_FUNC_GPIO &pcfg_pull_none>;
 		};
 	};
-
 	dp_hdmi {
 		// hdmi_hpdin_l: hdmi-hpdin-l {
 		// 	rockchip,pins = <1 RK_PA0 RK_FUNC_GPIO &pcfg_pull_none>;
@@ -863,41 +881,82 @@ &usbdp_phy0_dp {
 };
 
 &usbdp_phy0_u3 {
-	status = "okay";
+	status = "disabled";
 };
 
 &usbdp_phy1 {
 	//rockchip,dp-lane-mux = <3 2 1 0>;
-	rockchip,dp-lane-mux = <0 1 2 3>;
-	status = "okay";
 	//status = "disabled";
+	orientation-switch;
+	svid = <0xff01>;
+	sbu1-dc-gpios = <&gpio4 RK_PB7 GPIO_ACTIVE_HIGH>;
+	sbu2-dc-gpios = <&gpio3 RK_PB2 GPIO_ACTIVE_HIGH>;
+	status = "okay";
+
+	port {
+		#address-cells = <1>;
+		#size-cells = <0>;
+		usbdp_phy1_orientation_switch: endpoint@0 {
+			reg = <0>;
+			remote-endpoint = <&usbc1_orien_sw>;
+		};
+
+		usbdp_phy1_dp_altmode_mux: endpoint@1 {
+			reg = <1>;
+			remote-endpoint = <&dp1_altmode_mux>;
+		};
+	};
 };
 
-&usbdp_phy1_dp {
-	status = "okay";
-	//status = "disabled";
+/*&usbdp_phy1_dp {
+	//status = "okay";
+	status = "disabled";
 };
 
 &usbdp_phy1_u3 {
-	//maximum-speed = "high-speed";
-	status = "okay";
-	//status = "disabled";
-};
+	maximum-speed = "high-speed";
+	//status = "okay";
+	status = "disabled";
+};*/
 
 &usbdrd_dwc3_0 {
 	//dr_mode = "otg";
 	dr_mode = "peripheral";
-	maximum-speed = "high-speed";
-	extcon = <&u2phy0>;
 	phys = <&u2phy0_otg>;
 	phy-names = "usb2-phy";
+	//这个影响了 adb 识别, 如果没指定, 默认应该是usb3.0, 然后驱动会分配 lane0/1给 usb3, 那么 dp 的 lane 只能配两个,一开始配了四个,导致adb没识别
+	maximum-speed = "high-speed";
+	extcon = <&u2phy0>;
 	status = "okay";
 };
 
 &usbdrd_dwc3_1 {
-	dr_mode = "host";
-	maximum-speed = "high-speed";
-	phys = <&u2phy1_otg>;
-	phy-names = "usb2-phy";
+	//dr_mode = "host";
+	dr_mode = "otg";
+	//phys = <&u2phy1_otg>;
+	//phy-names = "usb2-phy";
+	//maximum-speed = "high-speed";
+	usb-role-switch;
 	status = "okay";
+
+	port {
+		#address-cells = <1>;
+		#size-cells = <0>;
+		dwc3_1_role_switch: endpoint@0 {
+			reg = <0>;
+			remote-endpoint = <&usbc1_role_sw>;
+		};
+	};
+};
+
+&dp1 {
+        status = "okay";
+};
+
+&dp1_in_vp1 {
+        status = "okay";
 };
+
+&dp1_in_vp2 {
+        status = "disabled";
+};
\ No newline at end of file
